import { ChapterProps } from "@src/components/Chapter";
import { RuleProps } from "@src/components/Rule";

import _ from "./_";

const SeqRules: RuleProps[] = [_];

const SeqChapter: ChapterProps = {
  id: "chapter-seq",
  title: "Seq",
  rules: SeqRules,
};

export default SeqChapter;
